// Browser stub for node:readline — used by posthog-node context-lines modifier (not called in browser).
export function createInterface() { return { on() {}, close() {} }; }
export default { createInterface };
